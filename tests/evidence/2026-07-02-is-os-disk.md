# 2026-07-02 — storage.physical_disks[].is_os_disk 추가

## 무엇 / 왜

- **무엇**: 각 물리 디스크에 OS 루트 설치 여부를 나타내는 `is_os_disk` (Nice, `boolean|null`, OS 채널 전용) 추가.
- **왜**: 호출자가 physical_disks 목록에서 OS 가 설치된 디스크를 식별할 수단이 없었음 (physical_disks / logical_volumes 어디에도 부재).
- **결정**: 사용자 승인 (2026-07-02). 적용 범위 = OS 채널 한정 / baseline 값 = 토폴로지 기반 채움 + 실장비 확인은 NEXT_ACTIONS.

## 구현

| 채널 | 판정 로직 |
|---|---|
| OS Linux | `findmnt -n -o SOURCE /` (fallback `/proc/mounts`) → `lsblk -s -n -o NAME,TYPE <src>` 로 하위 물리 디스크 역추적(파티션/LVM/mdadm/multipath). `OSDISK|/dev/<name>` emit → 정규화 시 membership. python_ok + raw fallback 동일 구현. root 불필요. |
| OS Windows | `%SystemDrive%` → `Get-Partition.DiskNumber` (부재 시 WMI `Win32_DiskPartition.DiskIndex` fallback) → `Win32_DiskDrive.Index` 매칭. 물리 매칭 0건(가상디스크 등)이면 `null`. |

- `true`=OS 루트 디스크(RAID/LVM 멤버 모두 true), `false`=OS 루트 아님, `null`=판정 불가(SAN/iSCSI/NFS 루트 또는 도구 부재 — 거짓 false 안 냄).
- 기준은 OS 루트(`/`); `/boot`·EFI 별도 디스크는 false. Dell BOSS-N1 같은 부팅 NVMe 는 그 NVMe 가 true.

## 변경 파일

- 코드: `os-gather/tasks/linux/gather_storage.yml`(python+raw), `os-gather/tasks/windows/gather_storage.yml`
- 스키마: `schema/field_dictionary.yml` (Nice 엔트리, `channel:[os]`)
- Baseline(4, 토폴로지 기반): `ubuntu`(sda=true,sdb=false) / `rhel810_raw_fallback`(sda=true,sdb=false) / `windows`(PHYSICALDRIVE0=true) / `windows_2022`(PHYSICALDRIVE0=true,1=false)
- 예시(4): `os_linux_ubuntu2404` / `os_linux_rhel810_raw_fallback` / `os_windows2022` / `os_linux_baremetal_dell`(nvme0n1=true — BOSS 부팅, sda≠OS 시연)
- 테스트: `tests/e2e/conftest.py`(OS_ARRAY_FIELDS), `tests/e2e/test_os_output.py`(`test_os_disk_flag`)
- 문서: `docs/20`(§6.3.0a) / `docs/16`(Linux·Windows 매핑) / `docs/09`(필드목록)

## 검증

- [PASS] `python tests/validate_field_dictionary.py` — RESULT: PASS (0 fail, is_os_disk 엔트리 인식, priority/channel 유효)
- [PASS] `python -m pytest tests/e2e/test_os_output.py -q` — 31 passed (신규 `test_os_disk_flag` + array 존재 검증 포함)
- [PASS] 4 OS baseline JSON `json.load` 유효 + 전 디스크 is_os_disk 존재
- [PASS] gather YAML `yaml.safe_load` 파싱 (linux/windows)
- [FAIL 불가/환경제약] `ansible-playbook --syntax-check` — Windows dev 에 ansible 미설치. **실장비 Agent 에서 검증 필요.**
- [미확인] 실 OS gather 실행(실장비)으로 is_os_disk 실값 true/false 확인 — 이 세션 불가. NEXT_ACTIONS 등재.

## schema_version

유지 (additive Nice — hba-ib +9 Nice 선례 "schema_version 유지 D3" 동일).

## 부수 발견 — field_dictionary 카운트 drift (pre-existing)

- 헤더/문서 다수가 "Must 47 / Nice 81 / Skip 6 = 134" 로 기재. **실측 파싱**: Must 47 / **Nice 115** / Skip 6 = **168** (is_os_disk +1 전 기준 nice 114 / 167).
- must(47)·skip(6) 은 문서와 일치, **nice 만 34 어긋남** → additive Nice 누적이 카운트 문서에 미반영된 pre-existing drift (본 변경 이전부터).
- 본 변경에서 `field_dictionary.yml` 헤더 + `docs/09` 를 실측(168)으로 정정. 나머지 카운트 기재 위치는 CONVENTION_DRIFT / NEXT_ACTIONS 참조.

## 감사 결과 (2026-07-02, is-os-disk-audit workflow — 4관점 27 findings)

실장비 측정 전 다관점(Linux/Windows/스키마·테스트/누락) + 적대적 검증 감사. 확정 버그 수정:

- **[HIGH][fix] Linux lsblk 트리 글리프**: `lsblk -s` 는 기본 트리 모드라 NAME 에 글리프(`└─`)를 붙여 `OSDISK|/dev/└─sda` 로 오염 → 실장비(LVM/파티션 root) 대부분에서 is_os_disk 가 **항상 false**(OS 디스크 never true) 되던 치명 버그. `-l`(list) 추가로 flat 출력 강제. python+raw 양 경로.
- **[MED][fix] btrfs SOURCE 접미사**: findmnt 가 btrfs 는 `/dev/sdaN[/@subvol]` 반환 → readlink/lsblk 실패 → null. `${src%%\[*}` 로 strip. python+raw.
- **[MED][fix] Windows Win32_DiskDrive 2회 열거**: 매칭·emit 각각 열거(perf/TOCTOU) → `$dd` 1회 캡처 공용 스냅샷.
- **[INFO][fix] Windows WQL 가드**: `%SystemDrive%` 를 `^[A-Za-z]:$` 검증 후 WQL filter 사용.
- **[MED][fix] schema/fields/os.yml 누락**: physical_disks item_fields 에 is_os_disk 추가 (rule 22 R2 step6).
- **[MED][fix] 카탈로그 stale**: SCHEMA_FIELDS.md(83→168) / FIELD_USAGE_MATRIX regen(is_os_disk 행) / PROJECT_MAP fingerprint --update / measurement-targets / rule92(count-agnostic) / output_examples README — DRIFT-018 확장.
- **[MED][fix] 테스트 갭**: windows_2022 + rhel810(raw 경로) `test_os_disk_flag` semantic 검증 추가.
- **[문서][fix] Windows software-RAID/Storage Spaces**: 가상 DiskNumber 라 전 디스크 null (Linux 멤버 all-true 와 비대칭) — field_dictionary help + docs/20 명시.

**PASS(무조치)**: scope/register(python·raw), membership/os_known gate, python-raw symmetry, readlink LVM, POSIX-sh raw, WQL(benign). VENDOR_ADAPTERS count 는 dated cycle-log(역사적) 보존.

**재검증**: validate_field_dictionary PASS(47/115/6), pytest e2e OS + unit **820 passed / 1 skipped**(신규 semantic 2건 포함), gather YAML parse OK.

**여전히 미확인(실장비 필요)**: `-l` fix 의 실 lsblk 출력 확인 / btrfs·mdraid·multipath·Storage Spaces 실측 / null 케이스 회귀 → NEXT_ACTIONS.
